{
  "name": "dasm-inspection",
  "private": true,
  "description": "DASM technical inspection app (Next.js + Supabase). Legacy Vite shell lives under legacy/vite-prototype/.",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
